{
  "templates": [
    {
      "id": "backend-only",
      "name": "Init a basic project",
      "description": "Minimal Base44 backend setup with entities, functions, and APIs",
      "path": "backend-only"
    },
    {
      "id": "backend-and-client",
      "name": "Bootstrap from an example",
      "description": "Full-stack example with Base44 backend, Vite and a React client application",
      "path": "backend-and-client"
    }
  ]
}
